2026-09-01T14:07:23+05:30
Purpose: verify the live Groq Qwen 3.6 JSON Object Mode transport with non-thinking reasoning_effort=none, one bounded real request, unchanged client-side EconomicIntentContract validation, and no relaxed Checkpoint A thresholds. This diagnostic cannot pass Checkpoint A by itself; it only gates the next frozen 80-case rerun.
